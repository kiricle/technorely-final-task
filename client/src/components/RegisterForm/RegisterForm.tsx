import { Input } from '../../ui/Input/Input';

export const RegisterForm = () => {
    return (
        <form>
            <Input
                content="Email"
                name="email"
            />
        </form>
    );
};
